package com.teamthree.event.repository;

import com.teamthree.event.domain.EventType;
import com.teamthree.event.dto.EventViewDto;
import org.springframework.data.jpa.repository.Query;

import java.util.List;

public class EventRepositoryImplementation {
    //public List<EventViewDto> getEventsByType()
    //{
        //@Query(value = "SELECT id, age_limit, date, description, max_ticket_number, name, sold_ticket_number, ticket_price, address_id, event_type FROM public.events", nativeQuery = true);
        //findEventsByEventType(EventType);
    //}
}
